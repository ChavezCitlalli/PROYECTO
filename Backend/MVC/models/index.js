const Producto = require ("./product.models");
const Usuarios = require ('./usuarios.models');
const Compras = require ('./compras.models');
const Detalle = require('./detalle.models');

module.exports = {
    Producto,
    Usuarios,
    Compras,
    Detalle
};
